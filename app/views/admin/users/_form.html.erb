 <% if @user.errors.any? %>
     <div id="error_explanation">
       <h2> <%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved: </h2>
       <ul>
         <% @user.errors.full_messages.each do |msg| %>
             <li> <%= msg %> </li>
         <% end %>
       </ul>
     </div>
 <% end %>

 <div class="form-group">
   <%= f.label :email, :class => "control-label" %>
   <%= f.text_field :email, :class => "form-control", :required => true %>
 </div>

 <% if @user.new_record? %>
     <div class="form-group">
       <%= f.label :password, :class => "control-label" %>
       <%= f.text_field :password, :class => "form-control", :required => true %>
     </div>

     <div class="form-group">
       <%= f.label :password_confirmation, :class => "control-label" %>
       <%= f.text_field :password_confirmation, :class => "form-control", :required => true %>
     </div>
 <% end %>

 <div class="form-group">
   <%= f.label :role, :class => "control-label" %>
   <%= f.select :role, options_for_select(User::ROLES, @user.role), {}, {:class => "form-control disabled user_roles"} %>
 </div>

 <script type="application/javascript">
     $(function()
     {
         $(".date_picker").datetimepicker({
             pickTime: false,
             useCurrent: true
         });

         $(".finance_type").change(function()
         {
             var f_type = $(".finance_type").val()
             $("#type").val(f_type)
         });
     })
 </script> 